{
  "name": "lacque-latte-website",
  "version": "1.0.0",
  "description": "Luxury nail studio website",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:debug": "playwright test --debug",
    "test:ui": "playwright test --ui"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.0",
    "playwright": "^1.40.0"
  }
}
